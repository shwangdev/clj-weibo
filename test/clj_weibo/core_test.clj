(ns clj-weibo.core-test
  (:require [clojure.test :refer :all]
            [clj-weibo.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest another-test
  (testing "I am ok" (is (= 1 1))))
